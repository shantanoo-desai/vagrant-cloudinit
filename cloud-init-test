#cloud-config

# non-sudo users
users:
    - name: user
      passwd: userpassword
      sudo: false

# install nginx
packages:
    - nginx

# add hostname
hostname: edgegrant

# ssh should have password auth
ssh_pwauth: yes
chpasswd:
    list: |
        user:userpassword

